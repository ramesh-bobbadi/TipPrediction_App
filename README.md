# TipPrediction_App
TipPrediction_App: Full-stack ML web application that predicts restaurant tip amounts using an XGBoost model. Built with React for the frontend and Django REST Framework for the backend. Integrated the trained model into Django for real-time predictions via REST APIs with a responsive UI.
